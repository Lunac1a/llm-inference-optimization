# 用户授权续跑3：补齐部分数据块

用户明确要求继续。原始冻结实现、780个输入、120秒请求超时、4GiB KV预算、单并发与停止条件保持不变，不追加优化。

- 先核验原始运行、resume-1和resume-2的快照与冻结文件。
- 已有1974个质量响应、48个固定输出计时保持原样。
- 依次补齐16K FP8第二块106个、BF16第二块130个、mixed第二块130个响应，共366个。
- 部分FP8块按已有响应ID跳过前24个样本，保持原顺序。重新启动服务后执行原有两次热身，不重跑已保存的3次正式计时。暂停中断的在途请求原始记录保留，新请求保存在resume-3。
- 其余两个数据块沿用原采集器，各增加3次计时；最终共2340个质量响应、54个计时。附加热身计入预算，不混入正式结果。
- 原累计6738.419663秒，剩余4061.580337秒。预算覆盖此前全部尝试和本次启动、热身、采集与关闭；不因续跑重置。
- 每组启动前显存占用须不超过2048MiB；内存至少2GiB、swap增量至多256MiB。临时防休眠在退出时释放。失败或预算触发即停，不自动重试或换题。
- 原始证据不修改。新增入口和报告在运行前冻结，输出独立保存于resume-3。
- 4K受待机影响、8K跨运行段。16K第一块位于resume-2；第二块FP8计时在resume-2，BF16/mixed在resume-3。最终报告明确跨段限制，不把合并延迟变化宣传为严格环境控制的普遍加速。
