# Mixed 单循环整块分流：最后一次分流候选

2026-09-18。用户授权再试一次，若无收益则停止分流写法并规划其他方向。

唯一变化：保留原始单 tile 循环；在读取区域内以计算块一致的运行时分支选择完整历史 FP8、完整当前 BF16 或跨界掩码读取。dot、causal mask、online softmax 和 acc 更新位于分支之后，只保留一份。当前 tile 的 scale 向量设为1，共用原计算语义。BLOCK_M=64、TILE_SIZE=32 和其他设置不变。

候选放在独立 `.local/research/mixed-branch-2026-09-18/candidate-source/`，不替换产品源码；原始对照来自上次试验验证过的 `baseline-source/`。实验环境变量 `INFERENCE_MIXED_BLOCK_SOURCE_BRANCH=1` 才开启，默认关闭。上一轮失败的三段静态展开不作为对照。

**数值、kernel、服务延迟、容量、质量、次数和阈值完全沿用 [上一轮冻结协议](mixed-split-protocol.md)**：20数值用例；3个性能形状、每形状20对AB/BA测量；micro比值几何均值≤0.97且最坏≤1.03才进入A/B/B/A服务测量；服务门槛通过才执行30题/实现独立质量检查。保留未使用的RULER序号5/6，不按答案选题。关闭开关必须逐元素等于原始源码。

记录编译器寄存器、spills、共享内存、PTX，并核实计算体是否仍被编译器复制；静态资源变化只能提供解释线索。普通性能测量不启动 profiler。每个统计保留全部原始样本。

本轮上限2400秒，micro上限600秒、服务单次上限450秒、请求上限120秒；计入阶段7200秒（已用258.614秒）。沿用内存/swap/故障停止条件，不重跑、不调门槛、不在失败后尝试第三个分流变体。若失败，交付结果及其他方向的有序计划，不擅自启动新方向实验。

运行前冻结本补充协议、上轮协议、候选/原始源码、实验脚本和输入；独立目录保存全部证据。执行入口 `benchmarks/latency/branch_run.py`，数值/独立kernel入口 `branch_micro.py`。
