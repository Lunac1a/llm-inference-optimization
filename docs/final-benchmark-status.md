# 最终 mixed 评测：已完成，保留中断历史

**最终状态：2340/2340正式质量响应、54/54固定输出延迟测量全部完成并核验。** 用户再次授权继续后，resume-3只补齐366个缺失响应，未重复已完成数据。原始及各次续跑冻结文件、暂停快照和原始SSE均通过核验。

累计GPU生命周期8985.00秒（149.75分钟），含此前失败及续跑；三小时预算尚余30.25分钟，不追加实验。服务端口、GPU compute进程与临时防休眠请求均已释放。最终结果、质量差与跨段计时限制见[完整报告](final-benchmark-results.md)，机器状态见[status.json](../benchmarks/results/final-2026-09-19/status.json)。以下内容均为历史记录，不代表当前仍处于暂停状态。

## 续跑2暂停检查点（历史）

最新状态（2026-09-19）：用户要求“暂停，明天继续”。已保存 **1974/2340** 个质量响应、**48/54** 个固定输出延迟响应；15个完整数据块和16K FP8第二块的24个已完成响应均保留。累计GPU生命周期 **6738.42秒（112.31分钟）**，原三小时预算剩余 **4061.58秒（67.69分钟）**。服务端口、GPU compute进程及临时防休眠请求均已释放。

续跑2在游戏退出后正常完成原先超时的8K BF16组及后续任务；不能据此断言显存争用是前两次超时的唯一原因。本段宿主日志未检出所检查的待机/恢复/显卡事件。此次停止是用户主动暂停，不是新超时。

下一次只补齐16K FP8第二块剩余106个响应、16K BF16第二块130个响应和16K mixed第二块130个响应，共366个。保留当前未完成请求的原始记录，不覆盖或重跑已经完成的响应。现有一次性续跑脚本不能直接重启，需新增支持部分数据块的续跑入口与汇总核验；不能删除`started.json`绕过限制。第二个16K数据块将跨运行时段，最终性能报告须明确这个限制。

检查点位于`.local/research/ruler-final-2026-09-19/resume-2/`：`completion.json`、`pause-validation.json`、`snapshot.json`、`ledger.jsonl`、`power-guard.json`及原始响应。公开状态见[status.json](../benchmarks/results/final-2026-09-19/status.json)。以下保留之前的中断历史。

## 首次运行历史

2026-09-19。**仓库整理与集成检查完成；扩大 benchmark 尚未完成。** 原协议和冻结实现未修改，没有自动重跑。

## 已完成

- 0.1.3 默认服务为 mixed，成功候选 kernel 原样集成；没有追加优化。
- 源码17项、干净安装包17项 CPU 回归通过，包内16文件与源码一致。新增统计/完整性检查2项通过，CI已加入对应步骤但未执行远端CI。
- GPU集成数值用例20/20通过。
- 780个新输入生成完毕，prompt哈希和QA问题与旧数据不重合。
- 正式质量响应 **1040/2340**，固定32-token延迟测量 **24/54**。已完成响应通过ID、usage、非空、SSE结束、零前缀命中/抢占核验。

| 长度 | BF16 Flash | 原生 FP8 | Mixed |
|---|---:|---:|---:|
| 4K | 260/260 | 260/260 | 260/260 |
| 8K | 0/260 | 130/260 | 130/260 |
| 16K | 0/260 | 0/260 | 0/260 |

## 停止原因与诊断边界

作业 `8192-0-bf16_flash` 的服务启动约20秒，短热身成功。随后固定32-token的8K长输入热身收到HTTP 200，但120秒内没有收到SSE数据，触发请求总超时。尚未开始本组正式质量请求，原始热身流文件为0字节。

引擎日志在等待期间显示一个运行中请求。短热身时出现过 Triton `_compute_slot_mapping_kernel` JIT告警，但这不足以认定后续长热身超时由编译导致。`EngineDeadError` 在任务已开始关闭引擎后出现，属于退出阶段记录，不能倒推为最初故障原因。日志中未发现可确认的前置OOM或具体kernel异常。

Windows Kernel-Power另记录现代待机事件（Australia/Sydney当地时间）：00:11:51进入、00:25:31退出；00:42:12进入、00:46:21退出。采集开始于第一段待机期间，第二段与8K采集重叠。存在设备状态干扰，因此本次耗时不应直接作为简历或稳定性能结论。没有证据把这些事件与00:54:37开始的超时热身建立确定因果关系。

未修改系统电源设置，未关闭其他应用或重置GPU。尚未确定根因。

## 保留与资源状态

- GPU进程生命周期合计 **2426.63秒（40.44分钟）**，原三小时预算剩余 **8373.37秒（139.56分钟）**。任何后续续跑须将已用时间计入，不重新获取完整三小时额度。
- 服务已退出，8035端口释放，结束检查无GPU compute进程。桌面仍可占用GPU显存，这不等于实验进程残留。
- 原始输入、响应、指标、日志、冻结源码和失败请求位于 `.local/research/ruler-final-2026-09-19/`。
- `pause-snapshot.json` 保存退出后文件哈希，`partial-validation.json` 保存已完成数据核验，`host-power-events.json` 保存主机事件。
- 公开精简状态：[status.json](../benchmarks/results/final-2026-09-19/status.json)。未生成冒充完整结果的总分报告。

下一步应先排查该热身停顿及设备待机干扰，再另行明确续跑协议。保留原失败，不覆盖原始结果；不能通过提高超时、换样本或重跑后只保留较快结果来掩盖本次问题。此时没有启动续跑。

## 用户授权续跑1：仍在相同热身超时

用户说明此前离开电脑并熄屏，随后明确要求继续。按[续跑协议](final-benchmark-resume-protocol.md)，核验原始文件后仅重试尚无正式响应的8K BF16数据块；原始实现、输入、120秒上限均保持不变。

Windows宿主成功持有临时SystemRequired和ExecutionRequired请求，未请求保持显示器点亮。短热身成功，8K长输入热身再次120秒无SSE返回，按规则停止；没有新增正式响应。累计GPU生命周期 **2574.68秒（42.91分钟）**，余 **8225.32秒（137.09分钟）**。端口和GPU compute进程已释放，临时电源请求已解除。

这次的只读诊断发现：

- 模型启动前GPU占用 **5148 MiB**；等待期间约 **15623 MiB**、GPU利用率100%、SM频率2355 MHz，进程仍运行。
- Windows GPU Process Memory计数器显示 **SlayTheSpire2** 使用约 **4153 MiB专用显存、708 MiB共享显存**，另有桌面合成器占用。进程计数器可能存在共享资源归属，不能简单相加成物理总用量。
- 防休眠生效后仍复现，不能把失败只归因于熄屏。显存争用是当前具体的环境干扰；尚未通过清除干扰后的运行确认其为唯一根因。
- 未关闭游戏或任何其他用户应用，已请求用户先保存并退出游戏。未自动开始下一次尝试。

续跑1的原始日志、账本、电源请求生命周期及GPU占用诊断保存在 `resume-1/`。原1040个正式响应保持不变；此前4K/8K性能限制仍成立。
