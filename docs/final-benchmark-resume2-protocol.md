# 第二次用户授权续跑

2026-09-19。用户暂停后再次明确要求继续。启动前检查：游戏进程已不存在，GPU占用约962 MiB，无GPU compute进程。此前模型启动前约5148 MiB的额外显存占用已消失。

- 原始段与续跑1完全保留，后者新增文件清单/哈希在本次GPU启动前冻结。
- 本次仅补齐原十个缺失数据块；原1040正式响应、24次延迟测量、20个数值用例均不重跑。
- 服务源码、模型、数据、采样、120秒请求上限和原组别顺序不变。
- 累计已用2574.675947199秒，三小时总预算剩余8225.324052801秒（137.09分钟）；本次所有启动/热身/退出继续计入。
- 每个数据块启动前检查GPU占用不超过2048 MiB，减少重复在显存争用环境运行的风险；超过即停止，不关闭用户应用。此为运行环境检查，不更改KV预算或模型配置。
- 临时SystemRequired/ExecutionRequired请求覆盖续跑进程生命周期；允许显示器关闭，结束时解除。
- 再次出现故障即停止，不自动第四次尝试。原失败不覆盖，完整报告包含全部账本。
- 4K/8K时间仍仅作有待机/跨段干扰的描述；16K三组均在本次段内运行，需结合宿主电源事件另行核验后报告。

入口：`resume2.py`（Linux/WSL），`keep_awake2.py`（Windows生命周期包装），`report_resumed2.py`（完整性核验及汇总）。结果目录为原评测根下`resume-2/`。
