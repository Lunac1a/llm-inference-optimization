# 最终评测续跑协议

2026-09-19。用户明确要求继续，并说明此前离开电脑、屏幕熄灭。此说明与待机记录一致，但不把超时根因直接认定为熄屏。

- 保留原始失败、1040个正式响应、24个延迟记录、90个冻结文件和退出快照。续跑前及结束后核验原文件哈希。
- 不改服务源码、输入、模型、精度、采样、120秒请求上限或资源配置，不再次运行已通过的数值检查或已完成数据块。
- 从失败的 `8192-0-bf16_flash` 重启一次；其原尝试没有正式质量响应，新的热身与正式记录放在 `resume-1/runs/`。随后按原顺序完成其余9个缺失数据块。再次出现故障就停止，不自动第三次尝试。
- 原GPU生命周期2426.630363868秒计入10800秒总额度，续跑剩余8373.369636132秒（139.56分钟），含启动、热身、采集、退出。
- Windows宿主进程在续跑期间持有临时 SystemRequired/ExecutionRequired 电源请求，允许显示器关闭；退出时解除，不永久修改电源计划，也不阻止用户主动休眠或关机。GPU启动前必须确认请求建立成功。
- 原有质量分数可在完整性核验后合并；保留前后分段标识。4K/8K时间含待机风险，不用于强性能结论，也不为取得更快结果而重测。16K三组均在续跑段完成，若期间无待机且检查通过，可单独报告其受控测量结果及边界。
- 全部2340质量响应、54次延迟记录完成后才生成完整质量报告。报告保留原失败与已用预算，不用删除失败的账本伪装一次成功。

新入口：`benchmarks/evaluation/resume.py`。原始脚本和协议保持不变；续跑脚本与本协议在启动前单独冻结。
