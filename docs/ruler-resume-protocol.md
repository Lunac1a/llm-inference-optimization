# RULER 用户暂停后的续跑补充

2026-09-18，用户要求继续。原始协议、输入、模型、推理实现、评分、题量 n=5 和 7200 秒阶段总预算保持不变；已用 338.363143078 秒计入总额。原始冻结清单不覆盖、不重写。

- 保留全部预检及 4K BF16 Flash 的 65 个正式响应。
- 4K BF16 Triton 已保存 31 个完整响应，只生成固定顺序中剩余 34 个；暂停时未形成完整结果的请求保留原始 SSE，续跑使用独立目录，不能混接流。
- 续跑前完整复制该半成品目录并记录 SHA256。续跑完成后将新结果追加至原结果文件，生成含两个分段来源的 summary。原始 SSE/指标不覆盖，新 SSE/指标位于独立分段目录。
- 后续 10 组沿用原始顺序与采集代码。额外启动、一次短热身和退出均计入同一预算，不重新预检、不增加题量、不根据答案调参。
- 新增续跑代码单独冻结；原始冻结文件逐项验证。任何新故障仍立即停止，不自动重试。
- 原账本最后一条 `completed, code=null` 是用户暂停引起，不能当成完成或模型失败。暂停说明保持可追溯。
- 跨天分段可能引入设备状态差异。4K BF16 Triton 分段延迟单独记录；本轮时间比较是探索性描述，不能视为严格配对的纯 kernel 加速倍率。
